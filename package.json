{
  "name": "social-network-workspace",
  "workspaces": [
    "network",
    "admin"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --workspace=network\" \"npm run dev --workspace=admin\"",
    "test": "npm run test --workspace=network"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
